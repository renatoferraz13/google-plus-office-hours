(function($) {
  $.fn.recentGooglePlusActivity = function(options) {

    var settings = $.extend({
      'configuration' : 'default'
    }, options);

    return this.each(function() {
      var $this = $(this);

      // Do stuff
    });
  };
})(jQuery);